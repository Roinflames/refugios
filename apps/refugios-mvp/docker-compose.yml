services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: refugios-mvp
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DATABASE_URL=${DATABASE_URL}
    restart: unless-stopped
