services:
  - type: web
    name: refugios-mvp
    runtime: node
    plan: starter
    buildCommand: npm install && npm run db:migrate
    startCommand: npm run start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: DATABASE_URL
        sync: false
